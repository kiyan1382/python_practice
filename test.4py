class dnode():
    def __init__(self, x):
        self.Data = x
        self.next = None
        self.back = None


class dlinked_list:
    def __init__(self):
        self.head = None

    def del_x(self, x):
        if self.head is None:
            print("error")
            return

        c = self.head

        # اگر نود اول بود
        if c.Data == x:
            self.head = c.next
            if self.head:
                self.head.back = None
            del c
            return

        while c:
            if c.Data == x:
                if c